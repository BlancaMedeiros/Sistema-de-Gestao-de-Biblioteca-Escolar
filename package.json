{
  "name": "sistema-de-gestao-de-biblioteca-escolar",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo do Sistema de Gestão de Biblioteca Escolar.",
  "scripts": {
    "dev": "docker compose up --build",
    "dev:down": "docker compose down",
    "dev:logs": "docker compose logs --follow",
    "dev:status": "docker compose ps",
    "build": "docker compose build",
    "build:backend": "npm --prefix backend run build",
    "build:frontend": "npm --prefix frontend run build",
    "build:all": "npm run build:backend && npm run build:frontend",
    "test:frontend": "docker compose run --rm frontend npm run test -- --watch=false",
    "test:backend": "npm test --prefix backend",
    "dev:migrate": "npm run migrate --prefix backend",
    "dev:seed:funcionarios": "npm run seed:funcionarios --prefix backend"
  },
  "packageManager": "npm@11.6.2"
}
